// ---------------------------------------------------------------------------
// 品牌全局配置 — 所有面向用户的名称/域名统一从这里读取
// 改名只需改此文件，代码中不要硬编码品牌名
// ---------------------------------------------------------------------------

/** 商城品牌名（邮件标题、欢迎语、页面 title 等） */
export const BRAND = "Yanan Store"

/** 邮件发件域名（@ 后面那部分，无需真实创建邮箱） */
export const EMAIL_DOMAIN = "mail.yanan.store"

/** 默认数据库名 */
export const DEFAULT_DB_NAME = "yanan"

/** 默认管理员邮箱 */
export const DEFAULT_ADMIN_EMAIL = "admin@yanan-test.com"

/** 默认管理员密码 */
export const DEFAULT_ADMIN_PASSWORD = "supersecret"
