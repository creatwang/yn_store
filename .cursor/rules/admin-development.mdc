---
description: Admin Dashboard (Vite + React Router + Hono RPC) 开发规则
globs: ["apps/admin/**/*"]
alwaysApply: false
---

# Admin 开发规则

## 方案 A（已定案）

- **开发入口**：仅 `apps/admin` → Vite 提供 HMR
- **拷贝源**：Medusa 源码（`apps/backend/node_modules/@medusajs/dashboard` 或官方仓库 tag），按需复制到 `apps/admin/src/`
- **禁止**：Vite alias 指向外部 dashboard 源码、在 Admin 中跑 Dashboard 的 `virtual:medusa/*`、使用 `@medusajs/js-sdk`

## 技术栈

- Vite + React 18 + React Router 6（`basename: '/admin'`）
- @medusajs/ui + @medusajs/icons
- TanStack Query v5 + TanStack Table v8
- Hono RPC（`hc<AppType>()`）
- React Hook Form + @hookform/resolvers + `@my-store/validators`

## API 调用

```typescript
// apps/admin/src/lib/api.ts
import { hc } from "hono/client"
import type { AppType } from "@my-store/server/app"

const client = hc<AppType>(import.meta.env.VITE_API_URL || "", { fetch: /* 注入 Bearer */ })
export const api = client.api // server 挂载在 app.route('/api', apiRoutes)
```

开发时 `VITE_API_URL` **留空**，走 Vite proxy `/api` → `localhost:9000`。  
直连后端：`http://localhost:9000`（**不要** 加 `/api` 后缀）。

列表/变更请求用 `parseJsonResponse()` 校验 `res.ok` 并解析 JSON。

## 目录

- `src/routes/` — 页面（主开发目录）
- `src/hooks/` — Hono RPC hooks（`use-products` 等）
- `src/components/` — 自有布局、鉴权
- `src/dashboard-ui/` — 从 Medusa 拷贝的块，接入 RPC 后可迁入 `components/`

## 从 Dashboard 拷贝组件

```bash
pnpm run copy:dashboard-ui   # 仓库根目录
```

拷贝后改写 `hooks/api` → `src/hooks/use-*.ts`，删除 js-sdk 引用。

## 路由

- `/login` — 登录
- `/` — 概览
- `/products`、`/products/new`、`/products/:id` — 商品

访问地址：`http://localhost:5173/admin/`
