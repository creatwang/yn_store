---
description: 外部 Agent / Trae 与本仓库协作时的强制阅读与约束（Hono 全栈 + 方案 A Admin）
globs: ["**/*"]
alwaysApply: true
---

# 协作前必读

1. 打开并遵循 **[docs/00-agent-handoff.md](docs/00-agent-handoff.md)**（完整流程、文档索引、检查清单）。
2. 功能与 UI 策略见 **[docs/07-feature-spec.mdx](docs/07-feature-spec.mdx)**。

# 技术栈（冻结）

- 后端：**Hono**（`apps/server`），前缀 `/api`，导出 `AppType` 供 RPC。
- ORM：**Drizzle**（`packages/db`），映射 Medusa 表，不改表结构。
- 校验：**Zod**（`packages/validators`），与 `@hono/zod-validator`、RHF 共用。
- Admin：**Vite + React Router + @medusajs/ui**，**仅** `apps/admin`；**方案 A**（拷贝 UI，禁止 js-sdk、禁止 alias dashboard 源码）。
- Store：**Astro**（`apps/storefront`）。
- 参考：**不修改** `apps/backend/`。

# 实现硬规则

- Hono 路由必须**链式**定义；子应用用 `.route()` 挂载到 `app.ts`。
- 所有列表/详情查询处理 **软删**（`deleted_at IS NULL`）。
- 新表仅 `custom_` 前缀；金额遵守 `amount` + `raw_amount`。
- 用户未明确要求时 **不要 git commit**。

# 文档优先级

若 `docs/00-architecture-overview.mdx` 或 `docs/04-implementation-plan.mdx` 与 **handoff / 07-feature-spec** 冲突，以 **handoff + 07** 为准。

# Skill

详细步骤与扩展说明：`.cursor/skills/hono-medusa-rebuild/SKILL.md`。
