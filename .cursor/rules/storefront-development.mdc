---
description: Astro Storefront 开发规则
globs: ["apps/store-web/**/*"]
alwaysApply: false
---

# Storefront 开发规则

## 技术栈
- Astro 5（SSR）
- Tailwind CSS
- 调用 Hono Store API：`/api/store/*`

## 数据获取
```astro
---
const res = await fetch(`${import.meta.env.PUBLIC_API_URL}/api/store/products`)
const { products } = await res.json()
---
```

## 页面
- `/` — 商品列表
- `/products/[handle]` — 商品详情

## 规范
- 静态区块用 `.astro`
- 交互组件（购物车等）用 React Islands
- 仅展示 `status = published` 商品
