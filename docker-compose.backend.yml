# 仅后台 API + Admin UI（/app）。需配置 DATABASE_URL、JWT_SECRET。
services:
  backend:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "7000:7000"
    env_file:
      - apps/server/.env
    environment:
      NODE_ENV: production
      PORT: 7000
